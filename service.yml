apiVersion: v1
kind: Service
metadata:
  name: evobus-i4q-bonding-app-service
  namespace: {{ .Values.Namespace }} 
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: evobus-i4q-bonding-app-service
